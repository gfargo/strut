# Secrets
.env
.env.local*
*.env
config/.vps.env
secrets/

# OS
.DS_Store

# vscode config
.vscode

# Logs
*.log
logs/

# 
.claude/

# Runtime data directories (created by deploy, must survive git clean)
stacks/*/data/
backups/
audits/
drift-history/
locks/

# Key material and discovery snapshots (managed by `keys` command)
stacks/*/keys/

# Runtime (user project content, not part of engine)
stacks/

# Archived generated docs/scripts
.archive/

# Wiki (cloned separately as git submodule)
.wiki

.www
.homebrew-tap
strut-action

# VHS recording output (regenerated, not committed)
bin/output/gif/*.gif
bin/output/png/*.png
!bin/output/**/.gitkeep
# Local live-tape recording credentials
.strut-live.env

# Timezone, e.g. America/New_York, Europe/London.
TZ=UTC

# Host path where your media library lives (movies, TV, music).
# Change this to the actual mount on your VPS.
MEDIA_PATH=./media

# Uncomment to publish an internal-network-only bind (Tailscale etc).
# JELLYFIN_BIND=127.0.0.1

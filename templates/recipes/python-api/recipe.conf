NAME=python-api
DESCRIPTION=FastAPI service + Postgres with health endpoint
SERVICES=api,postgres
REQUIRED_VARS=POSTGRES_PASSWORD,DATABASE_URL
DEFAULT_PORTS=8000,5432
TAGS=web,python,fastapi,postgres

# Password required to connect to the multiplayer game.
GAME_PASSWORD=change-me-pw

# Factorio version to run. "stable" tracks the latest stable release; pin
# (e.g. "1.1.101") if you want reproducible upgrades.
FACTORIO_VERSION=stable

# Save name — created on first boot if it doesn't exist.
SAVE_NAME=default

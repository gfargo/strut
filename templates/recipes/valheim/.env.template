# Display name shown in the Valheim server list.
SERVER_NAME=MyValheimServer

# Password required to connect (minimum 5 characters).
SERVER_PASS=change-me-pw

# World save name (files stored under the config volume).
WORLD_NAME=MyWorld

# Public/Community listing (1 = listed, 0 = private).
SERVER_PUBLIC=1

services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: STACK_NAME_PLACEHOLDER-uptime-kuma
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - uptime_kuma_data:/app/data

volumes:
  uptime_kuma_data:

# Public hostname / DNS name your agents will connect to. This becomes the
# server's identity in issued certificates; must match the URL you use.
HOSTNAME=mesh.example.com

# Set to 1 to disable MeshCentral's built-in Let's Encrypt (recommended when
# you put strut's own domain command in front — nginx / Caddy handles TLS).
USE_MINIWEBRTC=0

# The container serves TLS on 443 (published as 8083:443) — no HEALTH_PATH
# here so the health engine falls back to a TCP-only connect check instead
# of a plain-HTTP probe against a TLS port.
MESHCENTRAL_PORT=8083

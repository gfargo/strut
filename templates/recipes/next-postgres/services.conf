# web is expose:-only (never published to the host) — no WEB_PORT entry;
# the proxy in front of it is the reachable health-check surface.
PROXY_PORT=80

DB_POSTGRES=true

# web ships a Dockerfile and no published registry image — build it on the
# target host instead of pulling.
BUILD_MODE=local
